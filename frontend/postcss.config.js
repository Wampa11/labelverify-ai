/**
 * PostCSS pipeline for Tailwind and autoprefixer.
 * Architectural responsibility: CSS transform chain for the Vite frontend build.
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
